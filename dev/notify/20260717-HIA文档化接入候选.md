# HIA 文档化接入候选通知

## 背景

HIA-Documentation-Sys 已完成 UnicodeArtJs 的 TSDoc 目标项目接入候选件整理。本通知仅说明建议动作；HIA-Documentation-Sys 不直接修改 UnicodeArtJs 本体源码、配置、依赖或 CI。

## 候选件位置

候选件位于：

- `../HIA-Documentation-Sys/work-zone/temp/target-integration-candidates/current/UnicodeArtJs/`

包含：

- `package-json.merge.json`
- `docs/hia/hia-project-docs.candidate.json`
- `docs/hia/check-hia-tsdoc-adoption.cjs`
- `.github/workflows/hia-docs.yml`
- `README.md`

## 建议动作

1. 评估是否需要在 UnicodeArtJs 根 `package.json` 合并 `package-json.merge.json` 中的 HIA target docs scripts。
2. 如接受该 gate 命名，复制 `docs/hia/check-hia-tsdoc-adoption.cjs` 到项目内。
3. 如希望 CI 自动检查，按项目节奏吸收 `.github/workflows/hia-docs.yml`。
4. 运行既有 TSDoc gate 后，再运行候选 adoption check。

## 当前验证结论

- 当前项目已存在 `@hia-doc/tsdoc-runner@0.1.2`。
- 当前项目已存在 `tsdoc.core.json` 与 `tsdoc.vscode-extension.json`。
- HIA-Documentation-Sys 的候选件语法检查通过。
- HIA-Documentation-Sys 未修改 UnicodeArtJs 本体文件。

## 注意事项

本通知不要求立即改动。目标仓库可在自身规划周期内决定是否接入、如何命名 gate、是否启用 CI。
